import 'package:flutter/material.dart';

class DialogOverly extends StatelessWidget {
  const DialogOverly({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('dialog is here');
  }
}
